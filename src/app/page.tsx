import LandingPage from '@/components/landing/LandingPage';

export const metadata = {
  title: 'ReBuilt — Reconstruction cost, from a plan.',
  description: 'Upload a floor plan, get a precise rebuild cost. AI-powered reconstruction estimation for Belgian insurance companies.',
};

export default function Home() {
  return <LandingPage />;
}
